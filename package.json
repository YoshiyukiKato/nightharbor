{
  "name": "nightharbor",
  "description": "configure batch execution of lighthouse simply",
  "version": "0.5.2",
  "author": "Yoshiyuki Kato",
  "bin": {
    "nightharbor": "bin/cli.js",
    "nhb": "bin/cli.js"
  },
  "dependencies": {
    "cli-progress": "^2.0.0",
    "commander": "^2.17.0",
    "lighthouse": "^3.0.1",
    "puppeteer": "^1.7.0"
  },
  "devDependencies": {
    "@google-cloud/bigquery": "^1.3.0",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.9.4",
    "@types/power-assert": "^1.5.0",
    "@types/puppeteer": "^1.6.3",
    "@types/s3rver": "0.0.30",
    "aws-sdk": "^2.309.0",
    "csvtojson": "^2.0.8",
    "espower-typescript": "^9.0.1",
    "fs-extra": "^7.0.0",
    "intelli-espower-loader": "^1.0.1",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "power-assert": "^1.6.0",
    "s3rver": "^2.2.5",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  },
  "keywords": [
    "lighthouse",
    "multiple targets",
    "website performance",
    "reporting"
  ],
  "license": "MIT",
  "main": "index.js",
  "nyc": {
    "include": [
      "src/**/*.ts"
    ],
    "extension": [
      ".ts"
    ],
    "require": [
      "espower-typescript/guess"
    ],
    "reporter": [
      "text",
      "lcov"
    ],
    "sourceMap": true,
    "instrument": true
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/YoshiyukiKato/nightharbor"
  },
  "scripts": {
    "build": "$(npm bin)/tsc --build tsconfig.json",
    "watch": "$(npm bin)/tsc --build tsconfig.json -w",
    "test-with-cov": "$(npm bin)/nyc report --reporter=text-lcov > coverage.lcov npm test",
    "test": "$(npm bin)/nyc $(npm bin)/mocha --timeout 600000 --require intelli-espower-loader test/**/*.test.ts",
    "report-coverage": "codecov",
    "release": "npm run before-release && npm publish dist && npm run after-release ",
    "release-a": "npm run before-release && npm publish dist --tag alpha && npm run after-release ",
    "release-b": "npm run before-release && npm publish dist --tag beta && npm run after-release ",
    "before-release": "cp {README.md,package.json} dist",
    "after-release": "rm dist/{README.md,package.json}"
  }
}
